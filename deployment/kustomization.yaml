apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cicadia-system

resources:
  - storage.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: qmd